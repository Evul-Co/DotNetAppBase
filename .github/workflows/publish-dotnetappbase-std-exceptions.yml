name: Publish DotNetAppBase.Std.Exceptions

on:
  workflow_dispatch:
  push:
    tags:
      - "DotNetAppBase.Std.Exceptions-v*"

permissions:
  contents: read
  packages: write

jobs:
  publish_github_packages:
    uses: ./.github/workflows/publish-dotnet-package.yml
    with:
      project_path: src/DotNetAppBase.Std.Exceptions/DotNetAppBase.Std.Exceptions.csproj
    secrets: inherit

  publish_nugetorg:
    uses: ./.github/workflows/publish-nugetorg-package.yml
    with:
      project_path: src/DotNetAppBase.Std.Exceptions/DotNetAppBase.Std.Exceptions.csproj
    secrets:
      NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}
